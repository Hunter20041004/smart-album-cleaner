# ─── AI 表情相簿管家 — 依賴清單 ──────────────────────────────────────
# 安裝: pip install -r requirements.txt
# 已驗證可共存版本（2026-07，macOS Apple Silicon + Python 3.12）

# ── 核心:深度學習 ────────────────────────────────────────────────────
torch>=2.2,<3.0
torchvision>=0.17,<1.0

# ── 人臉偵測（支援中的 MediaPipe Tasks API）────────────────────────
mediapipe>=0.10.35,<0.11

# ── 影像處理 ─────────────────────────────────────────────────────────
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=2.0

# ── Web UI ───────────────────────────────────────────────────────────
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0

# ── 數據視覺化(confusion matrix / loss curves) ──────────────────────
matplotlib>=3.7
scikit-learn>=1.3   # confusion_matrix / classification_report

# ── 開發工具(可選,僅執行 tests 時需要) ─────────────────────────────
# 安裝: pip install -r requirements-dev.txt
